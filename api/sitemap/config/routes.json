{
  "routes": [
    {
      "method": "GET",
      "path": "/sitemap",
      "handler": "sitemap.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/sitemap",
      "handler": "sitemap.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/sitemap",
      "handler": "sitemap.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
